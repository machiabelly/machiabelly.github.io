/**
 * get an object properties
 *
 *
 */
import { ParamlessTypedJsNode } from './_Base';
import { JsLinesCollectionController } from './code/utils/JsLinesCollectionController';
export declare enum GetChildrenPropertiesJsNodeOutputName {
    position = "position",
    quaternion = "quaternion",
    scale = "scale",
    visible = "visible",
    castShadow = "castShadow",
    receiveShadow = "receiveShadow",
    frustumCulled = "frustumCulled",
    up = "up",
    matrixAutoUpdate = "matrixAutoUpdate"
}
/**
 *
 * We need different arrays per property.
 * Otherwise, a downstream node which would
 * query positions and scales would receive 2 identical arrays,
 * instead of 2 distinct ones
 *
 */
export declare class GetChildrenPropertiesJsNode extends ParamlessTypedJsNode {
    static type(): string;
    initializeNode(): void;
    setLines(shadersCollectionController: JsLinesCollectionController): void;
}
