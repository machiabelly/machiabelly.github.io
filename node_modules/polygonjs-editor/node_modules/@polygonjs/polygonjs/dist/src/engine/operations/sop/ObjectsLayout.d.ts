import { Vector2 } from 'three';
import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface ObjectsLayoutSopParams extends DefaultOperationParams {
    maxLayoutWidth: number;
    rowHeight: number;
    padding: Vector2;
    addAttribs: boolean;
    addRowAttrib: boolean;
    addRowWidthInner: boolean;
    addRowWidthOuter: boolean;
}
export declare class ObjectsLayoutSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: ObjectsLayoutSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<'objectsLayout'>;
    cook(inputCoreGroups: CoreGroup[], params: ObjectsLayoutSopParams): CoreGroup;
}
export {};
