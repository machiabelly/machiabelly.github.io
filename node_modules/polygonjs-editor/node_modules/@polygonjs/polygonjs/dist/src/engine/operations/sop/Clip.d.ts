import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { Vector3 } from 'three';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface ClipSopParams extends DefaultOperationParams {
    origin: Vector3;
    distance: number;
    direction: Vector3;
    intersectionEdges: boolean;
    keepBelowPlane: boolean;
    keepAbovePlane: boolean;
}
export declare class ClipSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: ClipSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.ALWAYS;
    static type(): Readonly<'clip'>;
    cook(inputCoreGroups: CoreGroup[], params: ClipSopParams): CoreGroup;
}
export {};
