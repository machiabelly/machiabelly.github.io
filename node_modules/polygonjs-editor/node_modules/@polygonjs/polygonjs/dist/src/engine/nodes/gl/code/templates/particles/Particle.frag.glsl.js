export default "varying vec4 vColor;\n\nvoid main() {\n\n	// float f = length( gl_PointCoord - vec2( 0.5, 0.5 ) );\n	// if ( f > 0.5 ) {\n	// 	discard;\n	// }\n	// gl_FragColor = vColor;\n	gl_FragColor = vec4( 1.0, 0.0, 1.0, 1.0 );\n\n\n}\n";
