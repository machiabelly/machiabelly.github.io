export interface CursorOffset {
    offsetX: number;
    offsetY: number;
}
export interface CursorPage {
    clientX: number;
    clientY: number;
}
export declare class MouseHelperClass {
    private static _instance;
    static instance(): MouseHelperClass;
    private constructor();
    private _rectByCanvas;
    setEventOffset(cursorPage: CursorPage, canvas: HTMLCanvasElement, offset: CursorOffset): void;
    private _resetCacheBound;
    private _resetCache;
}
export declare const MouseHelper: MouseHelperClass;
