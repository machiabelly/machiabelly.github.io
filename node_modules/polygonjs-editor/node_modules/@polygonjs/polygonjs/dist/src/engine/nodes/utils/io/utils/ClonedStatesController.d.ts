import { NodeContext } from '../../../../poly/NodeContext';
import { InputCloneMode } from '../../../../poly/InputCloneMode';
import { NodeInputsController } from '../InputsController';
export declare class ClonedStatesController<NC extends NodeContext> {
    private inputsController;
    private _clonedStates;
    private _clonedState;
    private _cloneRequiredStates;
    private _overridden;
    private node;
    constructor(inputsController: NodeInputsController<NC>);
    initInputsClonedState(states: InputCloneMode | InputCloneMode[]): void;
    overrideClonedStateAllowed(): boolean;
    cloneRequiredState(index: number): boolean | undefined;
    cloneRequiredStates(): boolean[];
    private _getCloneRequiredState;
    private _cloneRequiredFromState;
    overrideClonedState(state: boolean): void;
    overriden(): boolean;
    updateCloneRequiredState(): void;
}
