"use strict";
import { NamedFunction1 } from "./_Base";
export class cookNode extends NamedFunction1 {
  static type() {
    return "cookNode";
  }
  func(nodePath) {
    const node = this.scene.node(nodePath);
    if (node) {
      node.setDirty();
      node.compute();
    }
  }
}
