import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { Vector3 } from 'three';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface LookAtSopParams extends DefaultOperationParams {
    target: Vector3;
    up: Vector3;
    lerp: number;
    invertDirection: boolean;
}
export declare class LookAtSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: LookAtSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<'lookAt'>;
    cook(inputCoreGroups: CoreGroup[], params: LookAtSopParams): CoreGroup;
}
export {};
