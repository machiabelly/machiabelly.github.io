"use strict";
import { TypedPostNode } from "./_Base";
import { LuminancePass } from "postprocessing";
import { NodeParamsConfig } from "../utils/params/ParamsConfig";
class LuminanceParamsConfig extends NodeParamsConfig {
}
const ParamsConfig = new LuminanceParamsConfig();
export class LuminancePostNode extends TypedPostNode {
  constructor() {
    super(...arguments);
    this.paramsConfig = ParamsConfig;
  }
  static type() {
    return "luminance";
  }
  createPass(context) {
    const pass = new LuminancePass();
    return pass;
  }
}
