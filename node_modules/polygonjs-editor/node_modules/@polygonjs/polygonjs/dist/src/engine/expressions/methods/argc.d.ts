/**
 * Returns the number of arguments in a space-separated arguments list
 *
 * @remarks
 * It takes 1 argument.
 *
 * `argc(arguments)`
 *
 * - `<arguments` space-separated arguments list
 *
 * ## Usage
 *
 * - `argc('a b c')` - returns 3
 *
 */
import { BaseMethod } from './_Base';
export declare class ArgcExpression extends BaseMethod {
    static requiredArguments(): string[][];
    processArguments(args: any[]): Promise<any>;
}
