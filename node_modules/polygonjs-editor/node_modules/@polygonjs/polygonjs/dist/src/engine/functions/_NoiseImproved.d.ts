import { Vector3 } from 'three';
import { NamedFunction7 } from './_Base';
export declare class noiseImprovedVector3 extends NamedFunction7<[Vector3, number, Vector3, Vector3, number, number, number]> {
    static type(): string;
    func(position: Vector3, amp: number, freq: Vector3, offset: Vector3, octaves: number, ampMult: number, freqMult: number): number;
}
