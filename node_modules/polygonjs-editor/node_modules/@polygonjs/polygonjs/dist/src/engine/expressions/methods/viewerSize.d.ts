/**
 * Returns the size of the viewer.
 *
 * @remarks
 * It takes 0 arguments.
 *
 * `viewerSize()`
 *
 *
 * ## Usage
 *
 * - `viewerSize()` - returns a THREE.Vector2
 * - `viewerSize().x` - returns a float
 * - `viewerSize().y` - returns a float
 *
 */
import { BaseMethodFindDependencyArgs } from './_Base';
import { BaseMethod } from './_Base';
import { Vector2 } from 'three';
export declare class ViewerSizeExpression extends BaseMethod {
    static requiredArguments(): never[][];
    findDependency(indexOrPath: BaseMethodFindDependencyArgs): null;
    private _size;
    processArguments(args: any[]): Promise<Vector2>;
}
