import { NodeContext } from '../../../poly/NodeContext';
import { NodeBaseState } from './Base';
export declare class NodeTimeDependentState<NC extends NodeContext> extends NodeBaseState<NC> {
    active(): boolean;
    paramsTimeDependent(): boolean;
    inputsTimeDependent(): boolean;
    forceTimeDependent(): void;
    unforceTimeDependent(): void;
}
