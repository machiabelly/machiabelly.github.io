import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { Vector3 } from 'three';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface RingSopParams extends DefaultOperationParams {
    innerRadius: number;
    outerRadius: number;
    thetaSegments: number;
    phiSegments: number;
    open: boolean;
    angleStart: number;
    angleLength: number;
    direction: Vector3;
    center: Vector3;
}
export declare class RingSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: RingSopParams;
    static type(): Readonly<'ring'>;
    cook(inputCoreGroups: CoreGroup[], params: RingSopParams): CoreGroup;
    private _cookWithoutInput;
    private _createRingObject;
    private _createRequiredGeometry;
}
export {};
