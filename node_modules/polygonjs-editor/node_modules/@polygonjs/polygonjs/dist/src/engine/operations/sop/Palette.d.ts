import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../poly/InputCloneMode';
import { Color } from 'three';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface PaletteSopParams extends DefaultOperationParams {
    class: number;
    paletteName: string;
    colorsCount: number;
    color1: Color;
    color2: Color;
    color3: Color;
    color4: Color;
    color5: Color;
}
export declare class PaletteSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: PaletteSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<'palette'>;
    cook(inputCoreGroups: CoreGroup[], params: PaletteSopParams): CoreGroup;
    private _addAttribute;
    private _setObjectColor;
    private _setVertexColor;
    private _setVertexColorToObject;
}
export {};
