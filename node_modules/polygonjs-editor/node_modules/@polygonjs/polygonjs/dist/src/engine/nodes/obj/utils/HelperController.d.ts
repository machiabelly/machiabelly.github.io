import { Object3D } from 'three';
import { BaseLightHelper, BaseLightHelperObjNode } from './helpers/_BaseLightHelper';
export interface HelperConstructor<O extends Object3D, L extends Object3D> {
    new (node: BaseLightHelperObjNode<L>, name: string): BaseLightHelper<O, L, BaseLightHelperObjNode<L>>;
}
export declare class HelperController<O extends Object3D, L extends Object3D> {
    private node;
    private _helperConstructor;
    private _name;
    private _helper;
    constructor(node: BaseLightHelperObjNode<L>, _helperConstructor: HelperConstructor<O, L>, _name: string);
    initializeNode(): void;
    visible(): boolean;
    private _createHelper;
    update(): void;
}
