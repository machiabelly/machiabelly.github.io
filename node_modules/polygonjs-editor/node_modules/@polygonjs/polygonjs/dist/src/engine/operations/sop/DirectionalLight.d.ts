import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DirectionalLightParams, DirectionalLightContainer, DirectionalLightContainerParams } from '../../../core/lights/DirectionalLight';
import { NodeContext } from '../../poly/NodeContext';
export declare class DirectionalLightSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: DirectionalLightParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.NEVER;
    static type(): Readonly<'directionalLight'>;
    cook(_: CoreGroup[], params: DirectionalLightParams): CoreGroup;
    createLight(params: DirectionalLightContainerParams): DirectionalLightContainer;
    updateLightParams(container: DirectionalLightContainer, params: DirectionalLightParams): void;
    updateShadowParams<NC extends NodeContext>(container: DirectionalLightContainer, params: DirectionalLightParams): void;
}
