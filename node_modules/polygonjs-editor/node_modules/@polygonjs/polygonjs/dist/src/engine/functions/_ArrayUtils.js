"use strict";
import { createVariable } from "../nodes/js/code/assemblers/_BaseJsPersistedConfigUtils";
export function _matchArrayLength(src, target, _createElement) {
  _setArrayLength(target, src.length, _createElement);
}
export function _setArrayLength(target, length, _createElement) {
  if (target.length == length) {
    return;
  }
  if (target.length > length) {
    target.length = length;
  } else {
    const srcLength = length;
    for (let i = target.length; i < srcLength; i++) {
      target[i] = _createElement();
    }
  }
}
export function _matchArrayLengthWithType(src, target, type) {
  if (target.length == src.length) {
    return;
  }
  if (target.length > src.length) {
    target.length = src.length;
  } else {
    const srcLength = src.length;
    for (let i = target.length; i < srcLength; i++) {
      target[i] = createVariable(type);
    }
  }
}
