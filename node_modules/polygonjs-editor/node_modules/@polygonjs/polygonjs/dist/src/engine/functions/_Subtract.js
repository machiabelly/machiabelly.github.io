"use strict";
import { NamedFunction, NamedFunction2 } from "./_Base";
export class subtractNumber extends NamedFunction {
  static type() {
    return "subtractNumber";
  }
  func(...args) {
    let first = args[0];
    for (let i = 1; i < args.length; i++) {
      first -= args[i];
    }
    return first;
  }
}
export class subtractVector extends NamedFunction {
  static type() {
    return "subtractVector";
  }
  func(...args) {
    const first = args[0];
    for (let i = 1; i < args.length; i++) {
      first.sub(args[i]);
    }
    return first;
  }
}
export class subtractVectorNumber extends NamedFunction2 {
  static type() {
    return "subtractVectorNumber";
  }
  func(vector, number) {
    return vector.subScalar(number);
  }
}
