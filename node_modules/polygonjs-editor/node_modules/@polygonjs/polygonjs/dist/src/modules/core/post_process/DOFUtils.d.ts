import { Scene } from 'three';
export declare function updateObjectsWithDepthMaterial(scene: Scene, callback: () => void): void;
export declare function linearize(depth: number, near: number, far: number): number;
export declare function smoothstep(near: number, far: number, depth: number): number;
