import { BufferGeometry } from 'three';
import { Color } from 'three';
declare class MarchingCubes extends BufferGeometry {
    private resolution;
    private enableUvs;
    private enableColors;
    readonly isMarchingCubes = true;
    isolation: number;
    private size;
    private size2;
    private size3;
    private halfsize;
    private delta;
    private yd;
    private zd;
    private count;
    private maxCount;
    private field;
    private normal_cache;
    private palette;
    private positionArray;
    private normalArray;
    private uvArray;
    private colorArray;
    private _createPolygons;
    constructor(resolution: number, enableUvs: boolean, enableColors: boolean);
    createPolygons(): void;
    addBall(ballx: number, bally: number, ballz: number, strength: number, subtract: number, colors: Color | undefined): void;
}
declare const edgeTable: Int32Array;
declare const triTable: Int32Array;
export { MarchingCubes, edgeTable, triTable };
