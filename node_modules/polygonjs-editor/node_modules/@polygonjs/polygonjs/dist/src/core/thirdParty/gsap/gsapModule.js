"use strict";
import { GSAP_FACTORY } from "./gsapFactory";
import { gsap } from "./gsap";
export function onGsapModuleRegister(poly) {
  GSAP_FACTORY.gsap = gsap;
  GSAP_FACTORY.timeline = (vars) => {
    return gsap.timeline(vars);
  };
}
