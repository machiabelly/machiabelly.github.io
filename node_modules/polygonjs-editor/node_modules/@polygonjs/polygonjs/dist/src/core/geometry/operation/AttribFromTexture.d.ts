import { Texture } from 'three';
import { CoreObjectType, ObjectContent } from '../ObjectContent';
interface AttribFromTextureParams<T extends CoreObjectType> {
    object: ObjectContent<T>;
    texture: Texture;
    uvAttribName: string;
    targetAttribName: string;
    targetAttribSize: number;
    add: number;
    mult: number;
}
export declare class AttribFromTexture {
    setAttribute<T extends CoreObjectType>(params: AttribFromTextureParams<T>): void;
    private _dataFromTexture;
    private _dataFromDefaultTexture;
    private _dataFromDataTexture;
}
export {};
