"use strict";
export function withChildrenRemoved(object, callback) {
  const children = object.children;
  for (const child of children) {
    object.remove(child);
  }
  const result = callback();
  for (const child of children) {
    object.add(child);
  }
  return result;
}
