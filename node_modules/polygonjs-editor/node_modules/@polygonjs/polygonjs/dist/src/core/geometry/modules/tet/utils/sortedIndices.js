"use strict";
export function sortedNumber3(index0, index1, index2, target) {
  if (index0 < index1) {
    if (index1 < index2) {
      target[0] = index0;
      target[1] = index1;
      target[2] = index2;
    } else {
      if (index0 < index2) {
        target[0] = index0;
        target[1] = index2;
        target[2] = index1;
      } else {
        target[0] = index2;
        target[1] = index0;
        target[2] = index1;
      }
    }
  } else {
    if (index0 < index2) {
      target[0] = index1;
      target[1] = index0;
      target[2] = index2;
    } else {
      if (index1 < index2) {
        target[0] = index1;
        target[1] = index2;
        target[2] = index0;
      } else {
        target[0] = index2;
        target[1] = index1;
        target[2] = index0;
      }
    }
  }
}
export function sortedIndices(tetrahedron, facePointIndices, target) {
  const index0 = tetrahedron.pointIds[facePointIndices[0]];
  const index1 = tetrahedron.pointIds[facePointIndices[1]];
  const index2 = tetrahedron.pointIds[facePointIndices[2]];
  sortedNumber3(index0, index1, index2, target);
}
