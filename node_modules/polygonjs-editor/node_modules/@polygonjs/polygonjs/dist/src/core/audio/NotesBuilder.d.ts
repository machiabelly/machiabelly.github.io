export interface NoteHolder {
    octave: number;
    note: string;
}
export declare class NotesBuilder {
    static ALL_NOTES: string[];
    static list(startOctave: number, endOctave: number): NoteHolder[];
}
