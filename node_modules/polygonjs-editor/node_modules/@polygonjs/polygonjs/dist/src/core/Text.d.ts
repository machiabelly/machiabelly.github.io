import { DataTexture, Vector2, Color } from 'three';
interface WriteTextToCanvasOptions {
    texture: DataTexture;
    text: string;
    fontFamily: string;
    fontSize: number;
    resolution: Vector2;
    bgColor: Color;
    bgAlpha: number;
    textColor: Color;
    textAlpha: number;
}
interface LoadAndUseFontOtions extends WriteTextToCanvasOptions {
    fontUrl: string;
}
export declare function loadAndUseFont(options: LoadAndUseFontOtions): Promise<void>;
export {};
