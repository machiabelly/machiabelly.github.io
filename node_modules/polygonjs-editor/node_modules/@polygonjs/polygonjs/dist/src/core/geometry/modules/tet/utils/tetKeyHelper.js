"use strict";
const PRECISION = 6;
export function vector3ToStr(x, y, z) {
  return `${x.toFixed(PRECISION)}:${y.toFixed(PRECISION)}:${z.toFixed(PRECISION)}`;
}
export function string3ToStr(x, y, z) {
  return `${x}:${y}:${z}`;
}
