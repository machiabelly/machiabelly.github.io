import { Camera } from 'three';
import { PolyScene } from '../../engine/scene/PolyScene';
import { BaseNodeType } from '../../engine/nodes/_Base';
interface RenderSceneControllerOptions {
    scene: PolyScene;
    camera: Camera;
}
export declare class CoreCameraRenderSceneController {
    static isRenderSceneNode(node: BaseNodeType): boolean;
    static renderScene(options: RenderSceneControllerOptions): import("three").Scene | undefined;
}
export {};
