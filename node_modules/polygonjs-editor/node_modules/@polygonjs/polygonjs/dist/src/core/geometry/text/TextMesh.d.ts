import { Shape } from 'three';
export interface BevelParams {
    bevelEnabled: boolean;
    bevelThickness: number;
    bevelSize: number;
    bevelOffset: number;
    bevelSegments: number;
}
interface CommonParams extends BevelParams {
    extrude: number;
    curveSegments: number;
}
interface TextMeshesParams extends CommonParams {
    shapes: Array<Array<Shape>>;
}
export declare function createGeometriesFromTypeMesh(params: TextMeshesParams): (import("three").BufferGeometry<import("three").NormalBufferAttributes> | undefined)[];
export {};
