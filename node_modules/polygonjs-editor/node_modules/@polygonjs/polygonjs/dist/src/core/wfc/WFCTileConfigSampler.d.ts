import { TileConfig } from './WFCTileConfig';
export declare class WFCTileConfigSampler {
    private _cumulativeWeights;
    private _totalWeight;
    private _tileConfigs;
    setItemsAndWeights(tileConfigs: TileConfig[], weights: number[]): void;
    sample(seed: number): TileConfig;
}
