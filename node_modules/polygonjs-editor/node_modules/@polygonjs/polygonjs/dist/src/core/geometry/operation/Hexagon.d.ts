import { Vector2, BufferGeometry } from 'three';
export declare class CoreGeometryOperationHexagon {
    private _size;
    private _hexagonRadius;
    private _pointsOnly;
    constructor(_size: Vector2, _hexagonRadius: number, _pointsOnly: boolean);
    process(): BufferGeometry;
}
