import { BufferGeometry, Vector2 } from 'three';
import mapboxgl from 'mapbox-gl';
import { Vector2Like } from '../../../../types/GlobalTypes';
import { CoreObjectType, ObjectContent } from '../../../geometry/ObjectContent';
export declare class MapboxPlaneFrustumController<T extends CoreObjectType> {
    constructor();
    deleteOutOfView(map: mapboxgl.Map, object: ObjectContent<T>, plane_dimensions: Vector2, segments_counts: Vector2Like): BufferGeometry<import("three").NormalBufferAttributes> | null | undefined;
    private _triangle_a;
    private _triangle_b;
    private _point_pos;
    private _deleteOutOfView;
}
