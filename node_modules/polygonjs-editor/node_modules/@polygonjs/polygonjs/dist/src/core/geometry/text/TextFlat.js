"use strict";
import { ShapeGeometry } from "three";
import { mergeGeometries } from "three/examples/jsm/utils/BufferGeometryUtils";
export function createGeometriesFromTypeFlat(params) {
  var _a;
  return (_a = params.shapes) == null ? void 0 : _a.map((shapes) => createGeometryFromTypeFlat({ shapes }));
}
function createGeometryFromTypeFlat(params) {
  var _a;
  const geometries = ((_a = params.shapes) == null ? void 0 : _a.map((s) => new ShapeGeometry(s))) || [];
  if (geometries == null || geometries.length == 0) {
    return;
  }
  return mergeGeometries(geometries);
}
