import { TypedArray } from 'three';
import { Number3 } from '../types/GlobalTypes';
export interface HashOptions {
    spacing: number;
    maxNumObjects: number;
}
export declare class Hash {
    private spacing;
    private tableSize;
    private cellStart;
    private cellEntries;
    readonly queryIds: Int32Array;
    private querySize;
    constructor(options: HashOptions);
    hashCoords(xi: number, yi: number, zi: number): number;
    intCoord(coord: number): number;
    hashPos(pos: TypedArray, nr: number): number;
    create(pos: TypedArray): void;
    query(pos: Number3, nr: number, maxDist: number): void;
}
