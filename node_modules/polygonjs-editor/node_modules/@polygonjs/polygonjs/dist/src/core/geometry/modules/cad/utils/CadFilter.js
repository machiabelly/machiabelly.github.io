"use strict";
import { CoreType } from "../../../../Type";
import { CoreCadType } from "../CadCoreType";
export function cadFilterObjects(cadObjects, type) {
  if (!cadObjects) {
    return void 0;
  }
  if (CoreType.isArray(type)) {
    return cadObjects.filter((o) => type.includes(o.type));
  } else {
    return cadObjects.filter((o) => o.type == type);
  }
}
export function cadFilterShapes(cadObjects) {
  if (!cadObjects) {
    return void 0;
  }
  return cadObjects.filter((o) => CoreCadType.isShape(o));
}
