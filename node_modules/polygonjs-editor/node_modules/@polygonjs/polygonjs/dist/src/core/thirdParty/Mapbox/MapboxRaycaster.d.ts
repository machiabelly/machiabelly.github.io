import { Raycaster, Vector2 } from 'three';
import { MapboxPerspectiveCamera } from './MapboxPerspectiveCamera';
export declare class MapboxRaycaster extends Raycaster {
    private _inverseProjMat;
    private _camPos;
    private _mousePos;
    private _viewDir;
    setFromCamera(mouse: Vector2, camera: MapboxPerspectiveCamera): void;
}
