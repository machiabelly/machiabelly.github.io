import { Object3D } from 'three';
import { CorePositionalAudio } from './PositionalAudio';
export declare class CoreAudioListener extends Object3D {
    private context;
    private timeDelta;
    private _clock;
    private gain;
    listenerTransformAutoUpdate: boolean;
    constructor();
    addInput(positionalAudioNode: CorePositionalAudio): Promise<void>;
    dispose(): void;
    setMasterVolume(value: number): this;
    masterVolume(): number;
    updateMatrixWorld(force: boolean): void;
}
