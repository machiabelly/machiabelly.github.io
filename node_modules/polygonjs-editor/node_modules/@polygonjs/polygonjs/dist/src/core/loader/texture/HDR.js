"use strict";
import { ASSETS_ROOT } from "./../AssetsUtils";
import { BaseCoreImageLoader } from "./_BaseImageLoader";
import { RGBELoader } from "three/examples/jsm/loaders/RGBELoader";
export class HDRTextureLoader extends BaseCoreImageLoader {
  constructor(_url, _node) {
    super(_url, _node);
  }
  async _getLoader(options) {
    const loader = new RGBELoader(this.loadingManager);
    if (options.tdataType) {
      loader.setDataType(options.dataType);
    }
    return loader;
  }
}
HDRTextureLoader.PARAM_ENV_DEFAULT = `${ASSETS_ROOT}/textures/studio_small_09_2k.hdr`;
