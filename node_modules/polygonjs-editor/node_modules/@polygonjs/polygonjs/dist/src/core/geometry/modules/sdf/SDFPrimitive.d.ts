import { Vector3 } from 'three';
import { CorePrimitive } from '../../entities/primitive/CorePrimitive';
import { CoreObjectType, ObjectContent } from '../../ObjectContent';
import { SDFObject } from './SDFObject';
export declare class SDFPrimitive extends CorePrimitive<CoreObjectType.SDF> {
    constructor(object: SDFObject, index: number);
    geometry(): import("./manifold/manifold-encapsulated-types").Manifold;
    static entitiesCount<T extends CoreObjectType>(object: ObjectContent<T>): number;
    position(target: Vector3): Vector3;
    normal(target: Vector3): Vector3;
}
