"use strict";
export function dictionaryInvert(dict) {
  const invertedDict = {};
  const keys = Object.keys(dict);
  for (const key of keys) {
    const value = dict[key];
    invertedDict[value] = key;
  }
  return invertedDict;
}
