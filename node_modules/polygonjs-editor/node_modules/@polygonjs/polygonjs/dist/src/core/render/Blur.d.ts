import { WebGLRenderer, Vector2, WebGLRenderTarget } from 'three';
export declare class CoreRenderBlur {
    private _blurPlane;
    private _horizontalBlurMaterial;
    private _verticalBlurMaterial;
    private _renderTargetBlur;
    private _camera;
    constructor(res: Vector2);
    dispose(): void;
    setSize(w: number, h: number): void;
    private _createRenderTarget;
    private _createCamera;
    private _createBlurPlane;
    applyBlur(renderTarget: WebGLRenderTarget, renderer: WebGLRenderer, amountH: number, amountV: number): void;
}
