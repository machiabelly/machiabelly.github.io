import { WebGLRenderer } from 'three';
import { WebGLRenderTarget } from 'three';
import { DataTexture } from 'three';
export declare enum DataTextureControllerBufferType {
    Uint8Array = "Uint8Array",
    Uint8ClampedArray = "Uint8ClampedArray",
    Uint16Array = "Uint16Array",
    Float32Array = "Float32Array"
}
export declare class DataTextureController {
    private _dataTexture;
    private bufferType;
    constructor(bufferType?: DataTextureControllerBufferType);
    fromRenderTarget(renderer: WebGLRenderer, renderTarget: WebGLRenderTarget): DataTexture;
    reset(): void;
    private _copyToDataTexture;
    private _createDataTexture;
    private _createPixelBuffer;
    private _sameDimensions;
}
