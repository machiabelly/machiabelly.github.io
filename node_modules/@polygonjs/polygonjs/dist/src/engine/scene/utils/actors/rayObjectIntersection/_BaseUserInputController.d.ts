import { PolyScene } from '../../../PolyScene';
import { ActorsManager } from '../../ActorsManager';
import { Object3D } from 'three';
import { EmptyOptions } from './Common';
export declare class BaseUserInputController {
    protected actorsManager: ActorsManager;
    protected _scene: PolyScene;
    protected _objects: Object3D[];
    protected _propertiesListByObject: Map<Object3D, EmptyOptions[]>;
    constructor(actorsManager: ActorsManager);
    addPropertiesForObject(object: Object3D, properties: EmptyOptions): void;
    removePropertiesForObject(object: Object3D, properties: EmptyOptions): void;
}
