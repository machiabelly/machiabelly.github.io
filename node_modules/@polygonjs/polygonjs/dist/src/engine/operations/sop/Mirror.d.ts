import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { Vector3 } from 'three';
import { DefaultOperationParams } from '../../../core/operations/_Base';
import { SopType } from '../../poly/registers/nodes/types/Sop';
interface MirrorSopParams extends DefaultOperationParams {
    group: string;
    axis: Vector3;
    center: Vector3;
    preserveInput: boolean;
}
export declare class MirrorSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: MirrorSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<SopType.MIRROR>;
    cook(inputCoreGroups: CoreGroup[], params: MirrorSopParams): CoreGroup;
    private _applyOrCreateMirrorObject;
    private _mirrorObject;
}
export {};
