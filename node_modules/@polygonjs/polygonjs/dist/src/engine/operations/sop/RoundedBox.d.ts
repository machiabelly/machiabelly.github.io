import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { Vector3 } from 'three';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface RoundedBoxSopParams extends DefaultOperationParams {
    size: number;
    sizes: Vector3;
    divisions: number;
    bevel: number;
    center: Vector3;
}
export declare class RoundedBoxSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: RoundedBoxSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.NEVER;
    static type(): Readonly<'roundedBox'>;
    private _coreTransform;
    cook(inputCoreGroups: CoreGroup[], params: RoundedBoxSopParams): CoreGroup;
    private _cookWithoutInput;
    private _cookWithInput;
}
export {};
