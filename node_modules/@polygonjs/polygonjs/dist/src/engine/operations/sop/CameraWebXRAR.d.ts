import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
import { CameraSopNodeType } from '../../poly/NodeContext';
import { PolyScene } from '../../scene/PolyScene';
import { CoreObjectType, ObjectContent } from '../../../core/geometry/ObjectContent';
interface CameraWebXRARSopParams extends DefaultOperationParams {
    group: string;
    hitTest: number;
    lightEstimation: number;
    cameraAccess: number;
    overrideReferenceSpaceType: boolean;
    referenceSpaceType: number;
}
interface UpdateObjectOptions {
    scene: PolyScene;
    objects: ObjectContent<CoreObjectType>[];
    params: CameraWebXRARSopParams;
    active: boolean;
}
export declare class CameraWebXRARSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: CameraWebXRARSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<CameraSopNodeType.WEBXR_AR>;
    cook(inputCoreGroups: CoreGroup[], params: CameraWebXRARSopParams): CoreGroup;
    static updateObject(options: UpdateObjectOptions): void;
}
export {};
