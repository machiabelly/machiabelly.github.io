import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface CameraProjectSopParams extends DefaultOperationParams {
    project: boolean;
}
export declare class CameraProjectSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: CameraProjectSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<'cameraProject'>;
    private _pointPosition;
    cook(inputCoreGroups: CoreGroup[], params: CameraProjectSopParams): CoreGroup;
}
export {};
