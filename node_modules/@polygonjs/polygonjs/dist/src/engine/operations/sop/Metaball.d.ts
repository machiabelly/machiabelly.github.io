import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface MetaballSopParams extends DefaultOperationParams {
    resolution: number;
    isolation: number;
    useMetaStrengthAttrib: boolean;
    metaStrength: number;
    useMetaSubtractAttrib: boolean;
    metaSubtract: number;
    enableUVs: boolean;
    enableColors: boolean;
}
export declare class MetaballSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: MetaballSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.NEVER;
    static type(): Readonly<'metaball'>;
    cook(inputContents: CoreGroup[], params: MetaballSopParams): CoreGroup;
    private _createMetaballsGeometry;
}
export {};
