import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../poly/InputCloneMode';
import { SopType } from '../../poly/registers/nodes/types/Sop';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface UvLayoutSopParams extends DefaultOperationParams {
    res: number;
    padding: number;
    uv: string;
    uv2: string;
}
export declare class UvLayoutSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: UvLayoutSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<SopType.UV_LAYOUT>;
    cook(inputCoreGroups: CoreGroup[], params: UvLayoutSopParams): CoreGroup;
    private _layoutUVs;
}
export {};
