import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { Vector3 } from 'three';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface DecalSopParams extends DefaultOperationParams {
    t: Vector3;
    r: Vector3;
    s: Vector3;
    scale: number;
}
export declare class DecalSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: DecalSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.NEVER;
    static type(): Readonly<'decal'>;
    private _r;
    private _rotation;
    private _scale;
    cook(inputContents: CoreGroup[], params: DecalSopParams): CoreGroup;
}
export {};
