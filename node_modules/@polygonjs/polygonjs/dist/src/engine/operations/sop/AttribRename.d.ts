import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
interface AttribRenameSopParams extends DefaultOperationParams {
    group: string;
    class: number;
    oldName: string;
    newName: string;
}
export declare class AttribRenameSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: AttribRenameSopParams;
    static readonly INPUT_CLONED_STATE = InputCloneMode.FROM_NODE;
    static type(): Readonly<'attribRename'>;
    cook(inputCoreGroups: CoreGroup[], params: AttribRenameSopParams): CoreGroup;
}
export {};
