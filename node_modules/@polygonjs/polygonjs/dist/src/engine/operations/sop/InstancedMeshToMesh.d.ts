import { BaseSopOperation } from './_Base';
import { CoreGroup } from '../../../core/geometry/Group';
import { InputCloneMode } from '../../../engine/poly/InputCloneMode';
import { DefaultOperationParams } from '../../../core/operations/_Base';
import { SopType } from '../../poly/registers/nodes/types/Sop';
interface InstancedMeshToMeshSopParams extends DefaultOperationParams {
    cloneGeometry: boolean;
}
export declare class InstancedMeshToMeshSopOperation extends BaseSopOperation {
    static readonly DEFAULT_PARAMS: InstancedMeshToMeshSopParams;
    static readonly INPUT_CLONED_STATE: InputCloneMode[];
    static type(): Readonly<SopType.INSTANCED_MESH_TO_MESH>;
    cook(coreGroups: CoreGroup[], params: InstancedMeshToMeshSopParams): Promise<CoreGroup>;
}
export {};
