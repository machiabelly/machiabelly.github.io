"use strict";
import { NamedFunction3 } from "./_Base";
import { corePrimitiveClassFactory } from "../../core/geometry/CoreObjectFactory";
export class importPrimitiveAttributeNumber extends NamedFunction3 {
  static type() {
    return "importPrimitiveAttributeNumber";
  }
  func(object, attribName, index) {
    return corePrimitiveClassFactory(object).attribValue(object, index, attribName);
  }
}
