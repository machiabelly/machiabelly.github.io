"use strict";
import { NamedFunction, NamedFunction2 } from "./_Base";
export class divideNumber extends NamedFunction {
  static type() {
    return "divideNumber";
  }
  func(...args) {
    let first = args[0];
    for (let i = 1; i < args.length; i++) {
      first /= args[i];
    }
    return first;
  }
}
export class divideVectorNumber extends NamedFunction2 {
  static type() {
    return "divideVectorNumber";
  }
  func(vector, number) {
    return vector.divideScalar(number);
  }
}
