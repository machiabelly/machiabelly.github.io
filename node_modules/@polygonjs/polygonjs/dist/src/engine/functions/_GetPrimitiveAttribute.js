"use strict";
import { ObjectNamedFunction4 } from "./_Base";
import { primitiveClassFactoryNonAbstract } from "../../core/geometry/modules/three/ThreeModule";
export class getPrimitiveAttribute extends ObjectNamedFunction4 {
  static type() {
    return "getPrimitiveAttribute";
  }
  func(object3D, index, attribName, type, defaultValue) {
    const primitiveClass = primitiveClassFactoryNonAbstract(object3D);
    if (!primitiveClass) {
      return defaultValue;
    }
    const value = primitiveClass.attribValue(
      object3D,
      index,
      attribName
      /*, defaultValue as Vector3*/
    );
    if (value == null) {
      return defaultValue;
    }
    return value;
  }
}
