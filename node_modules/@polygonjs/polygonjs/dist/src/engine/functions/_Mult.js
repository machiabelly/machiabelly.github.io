"use strict";
import { NamedFunction, NamedFunction2 } from "./_Base";
export class multNumber extends NamedFunction {
  static type() {
    return "multNumber";
  }
  func(...args) {
    let first = args[0];
    for (let i = 1; i < args.length; i++) {
      first *= args[i];
    }
    return first;
  }
}
export class multVector extends NamedFunction {
  static type() {
    return "multVector";
  }
  func(...args) {
    const first = args[0];
    for (let i = 1; i < args.length; i++) {
      first.multiply(args[i]);
    }
    return first;
  }
}
export class multVectorNumber extends NamedFunction2 {
  static type() {
    return "multVectorNumber";
  }
  func(vector, number) {
    return vector.multiplyScalar(number);
  }
}
