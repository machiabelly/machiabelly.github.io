import { Camera, LineSegments } from 'three';
declare class CameraHelper extends LineSegments {
    readonly camera: Camera;
    private _pointMap;
    constructor(camera: Camera);
    clone(): this;
    update(): void;
}
export { CameraHelper };
