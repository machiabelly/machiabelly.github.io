"use strict";
import { CameraHelper } from "./CameraHelper";
export class CoreCameraHelper extends CameraHelper {
  clone(recursive) {
    return new CameraHelper(this.camera).copy(this, recursive);
  }
}
