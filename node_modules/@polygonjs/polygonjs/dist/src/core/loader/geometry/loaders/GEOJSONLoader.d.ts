import { LoadingManager, Loader, Group } from 'three';
import type { OnSuccess, OnProgress, OnError } from '../Common';
export declare class GEOJSONLoader extends Loader<Group> {
    constructor(manager: LoadingManager);
    load(url: string, onLoad: OnSuccess<Group>, onProgress?: OnProgress, onError?: OnError): void;
    private parse;
    private _parseGeojson;
    private _parseFeatureCollection;
    private _addAttributes;
    private _parseFeature;
    private _parseFeaturePoint;
    private _parseFeatureLineString;
    private _parseFeaturePolygon;
    private _parseFeatureMultiPolygon;
}
