import { PerspectiveCamera } from 'three';
import { UpdateProjectionOptions } from '../CoreCamera';
interface UpdateOptions extends UpdateProjectionOptions {
    cameraWithAttributes?: PerspectiveCamera;
}
export declare class CoreCameraPerspectiveFrameMode {
    static updateCameraAspect(camera: PerspectiveCamera, aspect: number, options?: UpdateOptions): void;
    private static _update;
    private static _adjustFOVFromModeDefault;
    private static _adjustFOVFromModeCover;
    private static _adjustFOVFromModeContain;
}
export {};
