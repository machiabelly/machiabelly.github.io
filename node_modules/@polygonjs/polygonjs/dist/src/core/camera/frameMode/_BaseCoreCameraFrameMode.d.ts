import { Camera } from 'three';
import { CameraFrameMode } from '../CoreCameraFrameMode';
export declare class BaseCoreCameraFrameMode {
    static frameMode(camera: Camera): CameraFrameMode;
    static expectedAspectRatio(camera: Camera): import("../../../types/GlobalTypes").AttribValue | undefined;
    private static _getFrameMode;
}
