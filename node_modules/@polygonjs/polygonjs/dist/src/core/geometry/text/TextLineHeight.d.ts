import { BufferGeometry } from 'three';
export interface TextLineHeightParams {
    lineHeight: number;
}
export declare function applyTextLineHeight(geometries: Array<BufferGeometry | undefined>, params: TextLineHeightParams): void;
