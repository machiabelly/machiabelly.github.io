"use strict";
export function prepareObject(object3D, options) {
  object3D.matrixAutoUpdate = false;
  object3D.updateMatrix();
  object3D.castShadow = options.shadow;
  object3D.receiveShadow = options.shadow;
}
