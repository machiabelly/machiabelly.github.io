import type { ManifoldStatic, Manifold, Mesh, Mat4, Smoothness, Box } from './manifold/manifold';
import Module from './manifold/manifold';
export type { ManifoldStatic, Manifold, Mesh, Mat4, Smoothness, Box };
export { Module };
import { Color } from 'three';
export type SDFGeometry = Manifold;
export declare enum SDFObjectType {
    DEFAULT = "SDFManifold"
}
export declare const SDF_OBJECT_TYPES_SET: Set<SDFObjectType>;
export interface SDFTesselationParams {
    facetAngle: number;
    meshesColor: Color;
    wireframe: boolean;
}
export interface SDFOBJTesselationParams {
    SDFFacetAngle: number;
    SDFMeshesColor: Color;
    SDFWireframe: boolean;
}
