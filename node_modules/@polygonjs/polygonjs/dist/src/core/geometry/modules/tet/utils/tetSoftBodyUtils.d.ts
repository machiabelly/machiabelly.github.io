import { TetGeometry } from '../TetGeometry';
export declare function buildTetIds(tetGeometry: TetGeometry, newOrderByPoint: Map<number, number>): number[];
export declare function buildTetEdgeIds(tetGeometry: TetGeometry, newOrderByPoint: Map<number, number>): number[];
