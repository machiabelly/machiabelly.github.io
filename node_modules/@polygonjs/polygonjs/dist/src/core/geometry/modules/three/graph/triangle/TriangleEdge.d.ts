import { TypedArray } from 'three';
import { PointIdPair } from './TriangleGraphCommon';
import { TriangleGraph } from './TriangleGraph';
export declare class TriangleEdge {
    readonly graph: TriangleGraph;
    readonly id: string;
    readonly pointIdPair: PointIdPair;
    triangleIds: number[];
    constructor(graph: TriangleGraph, id: string, pointIdPair: PointIdPair);
    addTriangle(triangleId: number): void;
}
export declare function triangleEdgeLength(edge: TriangleEdge, positions: TypedArray): number;
