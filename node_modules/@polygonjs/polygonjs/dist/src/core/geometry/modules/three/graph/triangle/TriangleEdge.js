"use strict";
import { Vector3 } from "three";
const _p0 = new Vector3();
const _p1 = new Vector3();
export class TriangleEdge {
  constructor(graph, id, pointIdPair) {
    this.graph = graph;
    this.id = id;
    this.pointIdPair = pointIdPair;
    this.triangleIds = [];
  }
  addTriangle(triangleId) {
    this.triangleIds.push(triangleId);
  }
}
export function triangleEdgeLength(edge, positions) {
  _p0.fromArray(positions, edge.pointIdPair.id0 * 3);
  _p1.fromArray(positions, edge.pointIdPair.id1 * 3);
  return _p0.distanceTo(_p1);
}
