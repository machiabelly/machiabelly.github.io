import { Vector2 } from 'three';
import { BufferGeometry } from 'three';
export interface CirclePositionOptions {
    radius: number;
    segments: number;
    arcAngle?: number;
}
export interface CircleCreateOptions extends CirclePositionOptions {
    connectLastPoint: boolean;
}
export declare class CoreGeometryUtilCircle {
    static positions(options: CirclePositionOptions): Vector2[];
    static create(options: CircleCreateOptions): BufferGeometry<import("three").NormalBufferAttributes>;
}
