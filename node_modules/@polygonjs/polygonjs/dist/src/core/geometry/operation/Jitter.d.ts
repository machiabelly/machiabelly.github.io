import { Vector3 } from 'three';
import { CoreGroup } from '../Group';
interface JitterOptions {
    amount: number;
    mult: Vector3;
    seed: number;
}
export declare function jitterOffset(i: number, seed: number, mult: Vector3, amount: number, target: Vector3): void;
export declare function jitterPositions(coreGroup: CoreGroup, options: JitterOptions): void;
export {};
