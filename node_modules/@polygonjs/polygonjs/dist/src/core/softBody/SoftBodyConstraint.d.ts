import { Vector3 } from 'three';
import { Number3 } from '../../types/GlobalTypes';
import { SoftBody } from './SoftBody';
export declare class SoftBodyConstraint {
    private _softBody;
    readonly pointIndex: number;
    readonly id: number;
    invMass: number;
    private _previousPosition;
    private _position;
    private _velocity;
    constructor(_softBody: SoftBody | null, pointIndex: number);
    dispose(): void;
    setPosition(position: Vector3, lerp: number, dt: number): void;
    velocity(target: Number3): void;
}
