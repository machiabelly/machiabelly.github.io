import { Vector3 } from 'three';
type DistanceFunction = (position: Vector3) => number;
export declare function softBodyRayMarch(origin: Vector3, dir: Vector3, maxDist: number, distanceFunction: DistanceFunction): number;
export {};
