"use strict";
export function coreGetDefaultCamera(scene) {
  var _a;
  return scene.root().mainCameraController.cameraSync() || ((_a = scene.viewersRegister.lastRenderedViewer()) == null ? void 0 : _a.camera()) || scene.root().mainCameraController.dummyPerspectiveCamera();
}
