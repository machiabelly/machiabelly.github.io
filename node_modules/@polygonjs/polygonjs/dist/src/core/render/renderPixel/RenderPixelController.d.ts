import { Object3D, Material, Vector2, Vector4, Camera, Color } from 'three';
import { PolyScene } from '../../../../src/engine/scene/PolyScene';
export declare function coreCursorToUv(cursor: Vector2, target: Vector2): void;
export declare class RenderPixelController {
    private _colorWriteRenderTarget;
    private _depthReadRenderTarget;
    private _renderScene;
    private _depthReadSetup;
    private _restoreContext;
    private _read;
    renderColor(scene: PolyScene, object3D: Object3D, material: Material | null, camera: Camera, backgroundColor: Color | null, uv: Vector2, target: Vector4): Vector4;
    renderDepth(scene: PolyScene, object3D: Object3D, camera: Camera, backgroundColor: Color | null, uv: Vector2, target: Vector4): Vector4;
    private _doRender;
    private _prepare;
    private _render;
    private _restore;
}
